<div local-class="wrapper">
  <button type="button" local-class="title-link" onclick={{action this.navigateToTrade}}>
    {{@trade.title}}
  </button>

  <div local-class="actions">
    <button type="button" local-class="action" onclick={{action @onDelete}}>
      <FaIcon @icon="trash-alt" />
    </button>
  </div>
</div>
