<div local-class="wrapper">
  {{#each this.folders as |folder index|}}
    <div local-class="folder-wrapper">
      <BtFavoritesTree::Folder
        @folder={{folder}}
        @onUpdate={{action this.updateFolder index}}
      />
    </div>
  {{/each}}
</div>

