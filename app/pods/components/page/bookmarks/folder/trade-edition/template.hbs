<Modal
  @title={{t "page.bookmarks.folder.trade-edition.modal-title"}}
  @onClose={{fn @onCancel}}
>
  <Form::Container
    @submitTask={{@submitTask}}
    @entity={{this.trade}}
    @canSubmit={{this.canSubmit}}
  >
    <Form::Input
      @label={{t "page.bookmarks.folder.trade-edition.title-label"}}
      @value={{this.trade.title}}
      @autofocus={{true}}
      @onChange={{fn this.changeTitle}}
    />

    <Button
      type="submit"
      @theme="gold"
      @label={{t "page.bookmarks.folder.trade-edition.save"}}
      @icon="save"
    />
  </Form::Container>
</Modal>
